
var word = 'Developer';

function orderLetters(text) {
    return text.split('').sort().join('');

    // split convert string to arry
    // sort sorts array
    // join convert back array to string

    // pitati prof OBAVEZNO
}


console.log(orderLetters(word));
console.log(word.typeOf);
